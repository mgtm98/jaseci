[project]
name = "{{name}}"
version = "0.1.0"
description = "Command-line Jac project"
entry-point = "main.jac"
kind = "cli"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"

[jacpack]
name = "cli"
description = "Command-line Jac project"
jaclang = "0.9.8"

[jacpack.options]
directories = [".jac"]
