[project]
name = "microservice-fixtures"
default-app = "order_service"

# Every module in this fixture is its own file-rooted service app, so the
# compiler classifies imports between them as service bridges and the tests
# can stand each one up as a provider. A discovery scan anchored at the parent
# fixtures/ tree treats this jac.toml as a hard boundary.

[apps.calculator_service]
kind = "service"
entry-point = "calculator_service.jac"
route = "/calculator"

[apps.dispatcher_service]
kind = "service"
entry-point = "dispatcher_service.jac"
route = "/dispatcher"

[apps.inventory_service]
kind = "service"
entry-point = "inventory_service.jac"
route = "/inventory"

[apps.math_service]
kind = "service"
entry-point = "math_service.jac"
route = "/math"

[apps.notify_service]
kind = "service"
entry-point = "notify_service.jac"
route = "/notify"

[apps.order_service]
kind = "service"
entry-point = "order_service.jac"
route = "/order"

[apps.level_a]
kind = "service"
entry-point = "level_a.jac"
route = "/level-a"

[apps.level_b]
kind = "service"
entry-point = "level_b.jac"
route = "/level-b"

[apps.level_c]
kind = "service"
entry-point = "level_c.jac"
route = "/level-c"
