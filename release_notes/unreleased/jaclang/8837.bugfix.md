- **Fix: calling a function stored in a `list[Callable]` crashed**: functions added to a list (via a literal, `append`, `insert`, or `lst[i] = f`) crashed or dispatched to the wrong function when called back. They now behave correctly.
