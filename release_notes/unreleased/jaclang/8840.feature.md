- **Empty lists, dicts, and sets now cost a single allocation on the native backend**: a freshly created empty container no longer allocates its element storage up front (an empty dict previously paid five allocations); buffers materialize on the first insert. Code that builds many containers that often stay empty, such as objects with rarely used side tables, allocates significantly less.
