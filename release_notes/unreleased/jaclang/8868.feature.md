- **Reference counting on the native backend no longer uses atomic operations**: retain/release and allocation bookkeeping now use plain memory updates instead of LOCK-prefixed atomic instructions, removing the dominant per-operation cost from refcount-heavy code. Native execution is single threaded per isolate, which is what makes this safe; a future design that shares refcounted objects across threads must bring atomic updates back or keep objects confined to one thread.
