- **Fix: building with many native module imports used excessive memory**: the native link kept every imported module's compiled IR and syntax tree in memory for the whole build. Each dependency's memory is now released as soon as its cache entry is written, so large multi-module builds no longer accumulate the footprint of every dependency at once.
